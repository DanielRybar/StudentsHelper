<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StudentsHelper.Views.SettingsPage"
             Title="Nastavení">
    <Grid Padding="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <VerticalStackLayout Style="{StaticResource SettingsLayout}">
            <Label Text="Režim aplikace" />
            <Picker ItemsSource="{Binding ThemesList}" SelectedItem="{Binding SelectedTheme}" />
        </VerticalStackLayout>
        <VerticalStackLayout Grid.Row="1" Style="{StaticResource SettingsLayout}">
            <Label Text="Výchozí záložka" />
            <Picker ItemsSource="{Binding ModulesList}" SelectedItem="{Binding SelectedModule}" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>